package tbdale.hpc.scala.condorAPI
/*
 * CondorCommands - function wrappers for HTCondor commands
 */

object CondorCommands {
   def submit(jobDescriptor:String):Long={
   ???
   }
   def remove(clusterID:Long)={
     
   }
}
